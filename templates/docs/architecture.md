# {{PROJECT_NAME}} — Architecture Reference

> Stable "how the system works" reference. Lower-volatility than `docs/status.md`, more detailed than `AGENTS.md`. AGENTS.md holds conventions; this holds feature specs, data flows, and contracts. Update when the architecture changes (and log the decision in `DECISIONS.md`).

## Repo layout

```
<tree of the meaningful dirs/files>
```

## <Subsystem / feature area>

<Specs, data flows, schemas, API contracts. One section per meaningful subsystem.>
