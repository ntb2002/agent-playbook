---
name: plan-phase
description: Deprecated — use /plan <project>. Kept for one sync cycle so old muscle memory doesn't fail silently.
disable-model-invocation: true
---

# /plan-phase $ARGUMENTS → use `/plan $ARGUMENTS`

This ritual merged into `/plan`. Run `/plan $ARGUMENTS` (project mode: decompose into session-sized issues filed into the tracker). Do nothing else here. This stub is deleted on the next playbook sync.
