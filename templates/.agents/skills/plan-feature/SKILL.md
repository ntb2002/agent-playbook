---
name: plan-feature
description: Deprecated — use /plan <issue-id>. Kept for one sync cycle so old muscle memory doesn't fail silently.
disable-model-invocation: true
---

# /plan-feature $ARGUMENTS → use `/plan $ARGUMENTS`

This ritual merged into `/plan`. Run `/plan $ARGUMENTS` (issue mode: expand one Deep-lane issue into `plans/features/`). Do nothing else here. This stub is deleted on the next playbook sync.
